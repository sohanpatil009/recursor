[package]
name = "agent-tools"
version = "0.1.0"
edition = "2021"

[dependencies]
agent-core = { path = "../agent-core" }
tokio = { version = "1", features = ["full"] }
serde_json = "1"
async-trait = "0.1"
tracing = "0.1"
regex = "1"
reqwest = { version = "0.12", features = ["json"] }
dunce = "1"
